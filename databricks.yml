bundle:
  name: crowdsourced-metadata-dash-app

resources:
  apps:
    crowdsourced-metadata-dash-app:
      name: 'crowdsourced-metadata-dash-app'
      source_code_path: .
      description: "Dash App for FEAllstars Crowdsourced Metadata"

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://e2-demo-field-eng.cloud.databricks.com